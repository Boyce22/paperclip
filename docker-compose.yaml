services:
  paperclip:
    build: .
    container_name: paperclip-agent
    ports:
      - "3100:3100"
    volumes:
      - paperclip-data:/data
      - ./prompts:/prompts
    restart: unless-stopped

volumes:
  paperclip-data: